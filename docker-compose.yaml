services:
  redis:
    image: redis:6-alpine
    restart: unless-stopped
    container_name: redis
    volumes:
      - redis_data:/data
    networks:
      - ome

  api:
    build: .
    image: my-app:latest
    command: bash -c "python store.py api"
    restart: unless-stopped
    depends_on:
      - redis
    env_file:
      - .common.env
    networks:
      - ome
    ports:
      - '8000:8000'

#  save_kp_series:
#    image: my-app:latest
#    command: bash -c "python store.py save-kp-series"
#    restart: unless-stopped
#    depends_on:
#      - redis
#    env_file:
#      - .common.env
#    networks:
#      - ome
#
#  bot:
#    image: my-app:latest
#    command: bash -c "python store.py bot"
#    restart: unless-stopped
#    depends_on:
#      - redis
#    env_file:
#      - .common.env
#    networks:
#      - ome

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./static:/var/www/one-more-episode.ru
    depends_on:
      - api
    networks:
      - ome

volumes:
  redis_data:

networks:
  ome: